# IMAS — Coming Soon

Página estática (HTML + CSS puro, sin frameworks) para imas.agency.

## Desarrollo local
Abre index.html directamente en el navegador, o usa una extensión tipo "Live Server" en VS Code.

## Deploy
Este proyecto no necesita build ni configuración especial: Vercel lo detecta como sitio estático automáticamente.